local function notify(text, duration)
local notif = Instance.new("Message", game:GetService("CoreGui"))
notif.Text = text
wait(duration)
notif:Destroy()
end

notify("Use the other script, this one is outdated.", 7.5)
